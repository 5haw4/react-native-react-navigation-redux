import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    wrapper: {
        padding: 15
    },
    countText: {
        textAlign: "center",
        padding: 5,
        fontSize: 20,
        fontWeight: "600"
    }
})