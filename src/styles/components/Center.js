import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    wrapper: {
        display: "flex",
        padding: 10,
        height: "100%"
    },
    centerVert: {
        justifyContent: "center",
    },
    centerHoriz: {
        alignItems: "center",
    }
})